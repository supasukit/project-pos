{
    "name": "pos-app",
    "version": "1.0.0",
    "main": "main.js",
    "scripts": {
  "start": "electron .",
  "dev": "vite",
  "build": "electron-builder",
  "build-win": "npx @electron/packager . pos-system --platform=win32 --arch=x64 --overwrite"
},

    "dependencies": {
        "electron": "^23.0.0",
        "gauge": "^5.0.2",
        "glob": "^11.0.1",
        "rimraf": "^6.0.1",
        "sqlite3": "^5.1.7"
    },
    "devDependencies": {
        "@electron/packager": "^18.3.6",
        "electron-reload": "^2.0.0-alpha.1",
        "vite": "^4.0.0"
    }
}
